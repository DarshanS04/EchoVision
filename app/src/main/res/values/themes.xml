<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme — dark, high contrast -->
    <style name="Theme.VisionAssist" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/accent_blue</item>
        <item name="colorPrimaryVariant">@color/bg_surface</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_green</item>
        <item name="colorSecondaryVariant">@color/accent_green</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:colorBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:fontFamily">sans-serif</item>
        <!-- Large touch targets for accessibility -->
        <item name="android:minHeight">48dp</item>
    </style>

    <!-- No action bar variant -->
    <style name="Theme.VisionAssist.NoActionBar" parent="Theme.VisionAssist">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Camera activity overlay style -->
    <style name="Theme.VisionAssist.Camera" parent="Theme.VisionAssist.NoActionBar">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

</resources>
